import wordsObj from '../assets/words_dictionary.json';

const words = Object.keys(wordsObj)

/**
 * 
 * @param {string[]} letters 
 * @returns {string[]}
 */
export function findWords(letters) {
    // TODO
    return words
}